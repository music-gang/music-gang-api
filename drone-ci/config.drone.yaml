# yaml-language-server: $schema=../schema.json

app:
  http: {}

  jwt:
    secret: secret
    expiresIn: 60 # minutes
    refreshExpiresIn: 20160 # minutes

  auths:

  redis:
    host: localhost
    port: 6379
    password:

  databases:
    postgres:
      database: ignore
      user: ignore
      password: ignore
      host: ignore
      port: 0000
test:
  http: {}

  jwt:
    secret: secret
    expiresIn: 60 # minutes
    refreshExpiresIn: 20160 # minutes

  auths:

  redis:
    host: redis-db
    port: 6379
    password:

  databases:
    postgres:
      host: postgres-db
      database: music-gang-ci-test
      user: postgres
      password: admin
      port: 5432
